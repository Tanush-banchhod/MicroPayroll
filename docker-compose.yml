version: "3.9"

services:
  db:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-micropayroll}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-micropayroll}
      POSTGRES_DB: ${POSTGRES_DB:-micropayroll}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-micropayroll}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-micropayroll}:${POSTGRES_PASSWORD:-micropayroll}@db:5432/${POSTGRES_DB:-micropayroll}
      REDIS_URL: redis://redis:6379/0
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./engine:/app/engine
      - ./api:/app/api
      - ./rules:/app/rules
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

  worker:
    build:
      context: .
      dockerfile: Dockerfile.api
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-micropayroll}:${POSTGRES_PASSWORD:-micropayroll}@db:5432/${POSTGRES_DB:-micropayroll}
      REDIS_URL: redis://redis:6379/0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./engine:/app/engine
      - ./api:/app/api
      - ./rules:/app/rules
    command: celery -A api.worker worker --loglevel=info

volumes:
  postgres_data:
